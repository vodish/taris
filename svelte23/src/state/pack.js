import * as store from "./store"



export function hpack( _href )
{
    // if ( typeof _href === 'object'  && _href.srcElement.tagName == "A")
    // {
    //     _href.preventDefault()
    //     _href    =   _href.srcElement.pathname
    // }
    
    // _href        =   parse(_href)
    

    // // параметры запроса к api
    // let data    =   { pack: _href.level[0], view: "html" }

    // if ( ! _href.level[1] )
    // {
    //     data.lineHtml   =   _href.level[0]
    // }
    // else if ( _href.level[1] == 'line' )
    // {
    //     data.lineText   =   _href.level[0]
    //     data.view       =   "line"
    // }
    // else if ( _href.level[1] == 'tree' )
    // {
    //     data.treeText   =   _href.level[0]
    //     data.view       =   "triee"
    // }
    // else if ( _href.level[1] == 'access' )
    // {
    //     data.accessText =   _href.level[0]
    //     data.view       =   "access"
    // }


    // api(data, (res) => {
        
    //     pack.set(res.pack)

    //     if ( res.lineHtml !== undefined )   { store.lineHtml.set(res.lineHtml) }
    //     if ( res.lineText !== undefined )   { store.lineText.set(res.lineText) }
    //     if ( res.treeText !== undefined )   { store.treeText.set(res.treeText) }
    //     if ( res.accessText !== undefined ) { store.accessText.set(res.accessText) }
        
        
    //     // view.set(data.view)
    //     store.href(_href.path)
        
    // })
    
}